# .env Guide

Copy `.env.example` and define:
- API keys (`FINNHUB_API_KEY`, `FMP_API_KEY`, etc.)
- MongoDB (`MONGODB_CONNECTION_STRING`, `MONGODB_DATABASE_NAME`)
- Firebase (`FIREBASE_PROJECT_ID`)
- Frontend (`VITE_API_URL`, `VITE_SIGNALR_URL`)

No secrets are hardcoded in the codebase.
